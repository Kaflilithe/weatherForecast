import { TranslateCityPipe } from './translate-city.pipe';

describe('TranslateCityPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateCityPipe();
    expect(pipe).toBeTruthy();
  });
});
